import React from 'react';

/*The Navigation creates the header with links to navigate
  between routes.
*/
const Navigation = () => (
  <header>
      <nav>
        <div>

        </div>
      </nav>
  </header>
)

export default Navigation;
